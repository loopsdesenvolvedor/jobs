import { IconType } from "react-icons";

interface LinkButtonWithIconProps {
  text: string;
  icon: IconType;
  href?: string;
}

const LinkButtonWithIcon = ({}: LinkButtonWithIconProps) => (
  <div>
    <div></div>
  </div>
);

export default LinkButtonWithIcon;
